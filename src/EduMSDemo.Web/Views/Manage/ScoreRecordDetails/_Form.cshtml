@model ScoreRecordDetailView

@{
    //String query = Request.QueryString.Count > 0 ? "?" + Request.QueryString : "";
    String controller = ViewContext.RouteData.Values["controller"] as String;
    String action = ViewContext.RouteData.Values["action"] as String;
    String id = ViewContext.RouteData.Values["id"] as String;
}

@using (Html.BeginForm(action, controller, new { area = "Manage", Id = id }, FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <div class="control-label col-sm-12 col-md-3 col-lg-2">
            @Html.FormLabelFor(model => model.ScoreRecordId)
        </div>
        <div class="control-content col-sm-12 col-md-9 col-lg-5">
            @Html.DropDownList("ScoreRecordId", null, new { @class = "form-control" })
        </div>
        <div class="control-validation col-sm-12 col-lg-5">
            @Html.ValidationMessageFor(model => model.ScoreRecordId)
        </div>
    </div>

    <div class="form-group">
        <div class="control-label col-sm-12 col-md-3 col-lg-2">
            @Html.FormLabelFor(model => model.MidTermScore)
        </div>
        <div class="control-content col-sm-12 col-md-9 col-lg-5">
            @Html.FormTextBoxFor(model => model.MidTermScore)
        </div>
        <div class="control-validation col-sm-12 col-lg-5">
            @Html.ValidationMessageFor(model => model.MidTermScore)
        </div>
    </div>

    <div class="form-group">
        <div class="control-label col-sm-12 col-md-3 col-lg-2">
            @Html.FormLabelFor(model => model.TermScore)
        </div>
        <div class="control-content col-sm-12 col-md-9 col-lg-5">
            @Html.FormTextBoxFor(model => model.TermScore)
        </div>
        <div class="control-validation col-sm-12 col-lg-5">
            @Html.ValidationMessageFor(model => model.TermScore)
        </div>
    </div>

    <div class="form-group">
        <div class="control-label col-sm-12 col-md-3 col-lg-2">
            @Html.FormLabelFor(model => model.FinalScore)
        </div>
        <div class="control-content col-sm-12 col-md-9 col-lg-5">
            @Html.FormTextBoxFor(model => model.FinalScore)
        </div>
        <div class="control-validation col-sm-12 col-lg-5">
            @Html.ValidationMessageFor(model => model.FinalScore)
        </div>
    </div>

    <div class="form-group">
        <div class="control-label col-sm-12 col-md-3 col-lg-2">
            @Html.FormLabelFor(model => model.DateOfScore)
        </div>
        <div class="control-content col-sm-12 col-md-9 col-lg-5">
            @Html.FormTextBoxFor(model => model.DateOfScore)
        </div>
        <div class="control-validation col-sm-12 col-lg-5">
            @Html.ValidationMessageFor(model => model.DateOfScore)
        </div>
    </div>

    <div class="form-group">
    </div>
    <div class="form-group">
        <div class="form-actions col-sm-12 col-lg-7">
            <button type="button" class="btn btn-default" onclick="window.history.back();">
                <i class="fa fa-chevron-left"></i>
                @Actions.Back
            </button>
            <button class="btn btn-primary" type="submit">
                <i class="fa fa-save"></i>
                @Actions.Submit
            </button>
        </div>
    </div>
}




